export const globalAnimationConfig = {
    // Entrance animation configs
    baseDelay: 0.15,        // Base delay between elements
    distance: 50,           // Default entrance distance
    duration: 0.6,          // Default entrance duration
    ease: "easeOut",        // Default easing
  
    // Flicker configs
    flickerDurationRange: [0.1, 0.15],  // Min and max flicker duration
    flickerRepeatsRange: [1, 5],       // Min and max flicker repeats
    flickerRandomDelay: 0.2,           // Maximum additional random delay
  };